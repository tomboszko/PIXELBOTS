const path = require('path');

module.exports = {
  entry: 'AICall.js', // replace with the path to your main JS file
  output: {
    filename: 'AICall.js',
    path: path.resolve(__dirname, 'AICall.js'), // replace 'dist' with the directory you want to output to
  },
};